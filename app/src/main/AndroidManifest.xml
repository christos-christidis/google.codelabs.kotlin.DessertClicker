<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kotlin.dessertclicker">

    <!-- SOS: Normally, app uses a default Application obj, here I change this to ClickerApplication -->
    <application
        android:name=".ClickerApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_dessert_clicker"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_dessert_clicker_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
